// Package config is the place to setup global variables and constants that impact site-wide behavior.
// Be very careful about calling functions that are in other packages from this package, as doing so
// can potentially create import loops, which Go does not allow.
package config
