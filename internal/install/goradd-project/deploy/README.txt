The build directory uses the deploy directory to stage files for processing before creating
final binaries and containers. You should not copy files to here, because these directories will
be recreated by the build scripts.