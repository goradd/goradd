{{
<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8"/>
</head>
<body>
}}


for _, db := range db.GetDatabases() {
    dd := db.Describe()
{{
<h1>{{= dd.DbKey }}</h1>
<ul>
}}
    for _, t := range dd.Tables {
        if !t.IsAssociation && !t.Skip {
            var key string = strings.Title(dd.DbKey)
            var formName = key + t.GoName + "Edit"
            var formLoc = t.GoName + "Edit"
{{
<li><a href="/form/{{= dd.DbKey }}/{{= formLoc }}">{{= formName }}</a></li>
}}
        }
    }
}

{{
</ul>
</body>
</html>
}}