//stringer.tmpl
{{

// String implements the Stringer interface and returns the default label for the object as it appears in html lists.
func (o *{{= t.GoName }}) String() string  {
    if o == nil {
        return ""
    }
{{if t.Options[db.StringerColumnOption] != nil}}
{{if col := t.GetColumn(t.Options[db.StringerColumnOption].(string)); col != nil}}
{{if col.ColumnType == query.ColTypeString}}
    return o.{{= col.ModelName() }}
{{else}}
    return fmt.Sprintf("%v", o.{{= col.ModelName() }})
{{if}}
{{if}}
{{elseif col := t.GetColumn("name"); col != nil}}
    return o.name
{{else}}
    return fmt.Sprintf("{{= t.GoName }} %v", o.PrimaryKey())
{{if}}
}

}}