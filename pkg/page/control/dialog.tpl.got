package control

import (
	"context"
	"bytes"
	"html"
)

func (d *Dialog) DrawTemplate(ctx context.Context, _w io.Writer) (err error) {
   {{err d.TitleBar().Draw(ctx, _w) }}

    l := len(d.Children())
    if l > 2 {
        for _,child := range d.Children() {
            {{err child.Draw(ctx, _w) }}
        }
    } else {
{{ {{!= d.Text() }} }}
    }
    {{err d.ButtonBar().Draw(ctx, _w) }}
	return
}
